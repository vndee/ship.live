{$APP_DOMAIN} {
	encode zstd gzip

	reverse_proxy app:3001 {
		# Deliver SSE frames immediately instead of buffering them at the proxy.
		flush_interval -1
	}

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		-Server
	}
}
